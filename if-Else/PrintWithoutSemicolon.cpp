// Write a C++ program to print something without using a semicolon (;).

#include <iostream>

int main() {
    if (std::cout << "Vikram") {
        // Empty block to satisfy the if statement
    }
}
