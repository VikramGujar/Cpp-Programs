// absolute_value.cpp
// Finding absolute value of given number [Always positive].
// Using the abs function

#include <iostream>
#include <cmath>

int main() {
    int n;
    std::cin >> n;
    std::cout << "Absolute Value of n is: " << abs(n) << std::endl;
    return 0;
}
